require 'spec_helper'

describe SimpleScrambler::Scramblers::ROT13 do 

  # describe ".scramble" do
  #   it { expect(subject.class.scramble("hello")).to eql("uryyb") }
  #   it { expect(subject.class.scramble("Hello")).to eql("uryyb") }

  #   it { expect(subject.class.scramble("hi")).to eql("uv") }
  #   it { expect(subject.class.scramble("this is a long testing message")).to eql("guvf vf n ybat grfgvat zrffntr") }
  # end
    
  # describe ".decrypt" do
  #   it { expect(subject.class.decrypt("uryyb")).to eql("hello") }
  #   it { expect(subject.class.decrypt("UrYyb")).to eql("hello") }
  # end

end